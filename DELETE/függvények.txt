Difficulty ->
	Difficulty diff = Difficulty.CUSTOM;
	diff.setValues(34, 22, 33);

Tile kirajzolása	
public void paintComponent(Graphics g) {
	super.paintComponent(g);
	t.draw(g);
}


Kép rajzolása
g.drawImage(Images.eight,10,10,null);
